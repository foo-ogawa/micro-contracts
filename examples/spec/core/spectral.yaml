# Core module Spectral rules
# Module-specific lint rules (extends shared rules)
extends:
  - ../_shared/spectral.yaml        # Inherit shared rules

rules:
  # Core module specific rules can be added here
  # Example: escalate warning to error for this module
  # error-response-problem-details:
  #   severity: error                # Override: warn → error

